import { useHelper } from '@react-three/drei'
import React, { useRef } from 'react'
import * as THREE from 'three'

const SpotLightHelper = ({position, firstTargetRef}) => {
    return (
        <>
            
        </>
    )
}

export default SpotLightHelper