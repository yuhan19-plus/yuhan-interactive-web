// 파일생성자 : 임성준
import store from "./store/store";

// 상태가 업데이트 될때 호출되는 리스너 정의 - 성준
// store.subscribe(() => {
//     console.log('current state : ', store.getState())
// })

// 사이드 메뉴 액션 디스패치 - 성준
// store.dispatch(sideMenuOpen())
// store.dispatch(sideMenuClose())